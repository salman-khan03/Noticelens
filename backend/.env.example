# Runtime label used for validation and operational context.
APP_ENV=development

# Exact browser origins allowed to call FastAPI directly. Comma-separated.
# Wildcards are rejected. Do not include paths or trailing slashes.
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
